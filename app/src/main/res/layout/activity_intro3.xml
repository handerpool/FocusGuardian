<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical"
    android:gravity="center_horizontal"
    android:background="@android:color/black"
    android:padding="24dp"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <ProgressBar
        style="?android:attr/progressBarStyleHorizontal"
        android:layout_width="match_parent"
        android:layout_height="4dp"
        android:progress="75"
        android:max="100"
        android:progressTint="@android:color/white"
        android:layout_marginBottom="40dp" />

    <TextView
        android:text="How old are you?"
        android:textColor="@android:color/white"
        android:textStyle="bold"
        android:textSize="22sp"
        android:gravity="center"
        android:layout_marginBottom="12dp"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"/>

    <TextView
        android:text="This is so we can estimate the amount of time you would spend on your phone over your life."
        android:textColor="#BBBBBB"
        android:textSize="16sp"
        android:gravity="center"
        android:layout_marginBottom="40dp"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"/>

    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1">

        <LinearLayout
            android:orientation="vertical"
            android:layout_width="match_parent"
            android:layout_height="wrap_content">

            <Button android:id="@+id/btnOption1" android:text="Under 14" style="@style/SelectableOption"/>
            <Button android:id="@+id/btnOption2" android:text="14–18" style="@style/SelectableOption"/>
            <Button android:id="@+id/btnOption3" android:text="19–25" style="@style/SelectableOption"/>
            <Button android:id="@+id/btnOption4" android:text="26–34" style="@style/SelectableOption"/>
            <Button android:id="@+id/btnOption5" android:text="35–44" style="@style/SelectableOption"/>
            <Button android:id="@+id/btnOption6" android:text="45–55" style="@style/SelectableOption"/>
            <Button android:id="@+id/btnOption7" android:text="56–65" style="@style/SelectableOption"/>
            <Button android:id="@+id/btnOption8" android:text="Over 65" style="@style/SelectableOption"/>

        </LinearLayout>
    </ScrollView>

    <Button
        android:id="@+id/btnContinue3"
        android:text="Continue"
        android:enabled="false"
        android:textColor="#999999"
        android:layout_marginTop="24dp"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@drawable/rounded_button_disabled"/>
</LinearLayout>
